package EX;
    public interface Resizable {
        void resize(double size);
    }
    //By defining this interface, we can create a common contract for any class that needs to be resized, regardless of its specific implementation. 
