//There're two cons of the clone method:
//It restricts direct access to the cloning process from outside the class hierarchy. which means that code is maintained through integrity 
//It allows subclasses to overrside the clone method with providing of other cloning processes