b. The File class can be used for any type of file, regardless of its content. However, the FileInputStream and FileOutputStream classes are typically used for binary files (e.g. images, audio files, executables), while the FileReader and FileWriter classes are used for text files.