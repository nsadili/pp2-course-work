import java.util.LinkedList;
import java.util.stream.Stream;

public class Main {
    public static void main(String[] args) {
        var list =m new LinkedList<>();
        list.add("Archie");
        list.add("X-men");
        Stream s - list.stream();
        s.forEach(System.out::ptintln);
        s.forEach(System.out::ptintln);
    }
}
